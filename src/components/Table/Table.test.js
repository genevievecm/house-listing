import React from 'react';
import ReactDOM from 'react-dom';
import Table from './Table';

/**
 * this test should check the component displays as expected
 */

describe('<Table />', () => {
	it('Renders a table header that is visually hidden', () => {});
	it('Renders children table elements', () => {});
});
