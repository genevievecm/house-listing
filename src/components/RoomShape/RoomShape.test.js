import React from 'react';
import ReactDOM from 'react-dom';
import RoomShape from './RoomShape';

/**
 * this test should check the component displays as expected
 */

describe('<RoomShape />', () => {
	it('Renders a block element with given height and width', () => {});
	it('Renders children text', () => {});
});
