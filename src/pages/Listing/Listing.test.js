import React from 'react';
import ReactDOM from 'react-dom';
import Listing from './Listing';

/**
 * this test should check the page component renders essential items
 */

describe('<Listing />', () => {
	it('Shows loading text until data is available', () => {});
	it('Renders 2 hero images', () => {});
	it('Renders a price header', () => {});
	it('Renders room profiles', () => {});
	it('Renders a table of rooms', () => {});
});
