import React from 'react';
import ReactDOM from 'react-dom';
import { formatPrice } from '../formatPrice';

/**
 * this test should check the function formats a price as expected
 */

describe('function to format currency in CAD', () => {
	it('Returns a six figure number formatted with commas', () => {});
	it('Returns a 10 figure number formatted with commas', () => {});
	it('Returns undefined if number param is not provided', () => {});
	it('Returns formatted number with max two decimal places', () => {});
});
